import AuthScreen from '@/components/AuthScreen';

export const metadata = {
  title: 'Register | Visit India',
};

export default function RegisterPage() {
  return <AuthScreen mode="register" />;
}
